# Cursor Todo.md
# 任務：PrintForm_StudentScoreCertificattion.cs 支援儲存 3 個樣板，並手動選擇 1 個列印
# 原則：保守、安全、最小影響範圍，不修改既有 MailMerge / 成績資料流 / PDF 與單檔輸出主邏輯
# 完成後記錄：國小在校成績證明書調整0421.md

---

## 一、修改檔案

1. PrintForm_StudentScoreCertificattion.cs
2. PrintForm_StudentScoreCertificattion.Designer.cs

---

## 二、修改目標

目前程式是單一樣板：
- 只有一個 Preference
- 只有一個 Template
- 列印固定使用 Preference.Template
- 樣板設定也只會改同一份 Template

本次要改成：
1. 可獨立儲存 3 個樣板
2. 畫面可手動選擇 低年級 / 中高年級 / 自訂報表
3. 列印時依目前選擇樣板輸出
4. 樣板設定按鈕要設定「目前選到的樣板」
5. 不修改既有報表資料整理、Merge、PDF/Word、單檔輸出主流程

---

## 三、保守設計方式

### 不要做的事
不要直接把原本：
- Preference.Template
改成陣列、List、Dictionary。
不要修改 Campus.Report / ReportPreference 底層結構。

### 建議做法
保留原本：
- Preference：繼續只管理列印相關設定（例如 PDF、OneFileSave 等）

另外新增：
- TemplatePreference1
- TemplatePreference2
- TemplatePreference3

再加：
- ComboBox 讓使用者手動選擇目前要使用哪個樣板

這樣風險最低。

---

## 四、PrintForm_StudentScoreCertificattion.cs 需新增成員

### 在 class 欄位區新增

```csharp
private ReportPreference TemplatePreference1 { get; set; }
private ReportPreference TemplatePreference2 { get; set; }
private ReportPreference TemplatePreference3 { get; set; }

private int SelectedTemplateIndex = 1;
````

### 說明

* Preference 保留原本用途，不拿來存 3 份樣板
* 三份 TemplatePreference 專門負責三份樣板
* SelectedTemplateIndex 表示目前使用哪個樣板（1~3）

---

## 五、建構式初始化 3 個樣板設定

### 找到原本類似這段

```csharp
Preference = new ReportPreference(ConfigName, Prc.學生在校成績證明書);
```

### 保留原本 Preference，並新增三份樣板 Preference

```csharp
Preference = new ReportPreference(ConfigName, Prc.學生在校成績證明書);

TemplatePreference1 = new ReportPreference(ConfigName + "_Template1", Prc.學生在校成績證明書);
TemplatePreference2 = new ReportPreference(ConfigName + "_Template2", Prc.學生在校成績證明書);
TemplatePreference3 = new ReportPreference(ConfigName + "_Template3", Prc.學生在校成績證明書);
```

### 注意

* ConfigName + "_Template1" / "_Template2" / "_Template3" 必須是不同 key
* 避免三個樣板互相覆蓋

---

## 六、新增取得目前樣板 Preference 的方法

### 在 cs 檔內新增方法

```csharp
private ReportPreference GetCurrentTemplatePreference()
{
    switch (SelectedTemplateIndex)
    {
        case 2:
            return TemplatePreference2;
        case 3:
            return TemplatePreference3;
        case 1:
        default:
            return TemplatePreference1;
    }
}
```

### 目的

不要在多處直接寫 if/else。
之後樣板設定與列印都統一走這個方法。

---

## 七、新增安全取得樣板 Stream 方法

### 在 cs 檔內新增方法

```csharp
private Stream GetCurrentTemplateStream()
{
    ReportPreference current = GetCurrentTemplatePreference();

    if (current != null && current.Template != null)
        return current.Template.GetStream();

    return new MemoryStream(Prc.學生在校成績證明書);
}
```

### 目的

避免直接寫：

```csharp
Preference.Template.GetStream()
```

造成 Template 為 null 時 crash。

### 注意

需確認檔案上方已引用：

```csharp
using System.IO;
```

如果原本沒有，請補上。

---

## 八、修改列印時載入樣板的地方

### 搜尋類似這段

```csharp
document = new Document(Preference.Template.GetStream());
```

### 改成

```csharp
document = new Document(GetCurrentTemplateStream());
```

### 目的

列印時改成依目前使用者選擇的樣板載入，而不是永遠固定單一樣板。

### 注意

只改樣板來源，不要動其他 MailMerge 與輸出邏輯。

---

## 九、修改樣板設定按鈕事件

### 搜尋類似這段

```csharp
private void lnkTemplate_LinkClicked(object sender, LinkLabelLinkClickedEventArgs e)
{
    ReportTemplate defaultTemplate = new ReportTemplate(Prc.學生在校成績證明書, TemplateType.Word);
    TemplateSettingForm form = new TemplateSettingForm(Preference.Template, defaultTemplate);
    ...
    if (form.ShowDialog() == DialogResult.OK)
    {
        Preference.Template = (form.Template == defaultTemplate) ? null : form.Template;
        Preference.Save();
    }
}
```

### 改成以下概念

```csharp
private void lnkTemplate_LinkClicked(object sender, LinkLabelLinkClickedEventArgs e)
{
    ReportTemplate defaultTemplate = new ReportTemplate(Prc.學生在校成績證明書, TemplateType.Word);

    ReportPreference currentTemplatePreference = GetCurrentTemplatePreference();

    TemplateSettingForm form = new TemplateSettingForm(currentTemplatePreference.Template, defaultTemplate);
    form.DefaultFileName = "在校成績證明書(樣版).doc";

    if (form.ShowDialog() == DialogResult.OK)
    {
        currentTemplatePreference.Template = (form.Template == defaultTemplate) ? null : form.Template;
        currentTemplatePreference.Save();
    }
}
```

### 重點

* 樣板設定按鈕改成設定「目前選到的樣板」
* 不再固定改 Preference.Template
* 每一份樣板各自 Save()

---

## 十、Designer 新增樣板選擇控制項

### 在 PrintForm_StudentScoreCertificattion.Designer.cs 新增控制項

1. Label：lblTemplateSelector
2. ComboBox：cboTemplateSelector

### Label 文字

```text
使用樣板：
```

### ComboBox 項目

```text
低年級
中高年級
自訂報表
```

### ComboBox 預設

```csharp
SelectedIndex = 0;
```

### 控制項命名建議

```csharp
private System.Windows.Forms.Label lblTemplateSelector;
private System.Windows.Forms.ComboBox cboTemplateSelector;
```

### 位置建議

放在：

* 報表樣版設定 link 附近
* 或列印格式選項下方

### 注意

* 不要蓋到原有控制項
* 必要時略微增加表單高度

---

## 十一、ComboBox 初始化與事件

### 在建構式或 Load 事件中初始化

```csharp
cboTemplateSelector.SelectedIndex = 0;
SelectedTemplateIndex = 1;
```

### 新增 SelectedIndexChanged 事件

```csharp
private void cboTemplateSelector_SelectedIndexChanged(object sender, EventArgs e)
{
    if (cboTemplateSelector.SelectedIndex < 0)
        return;

    SelectedTemplateIndex = cboTemplateSelector.SelectedIndex + 1;
}
```

### Designer 綁定事件

確認 `cboTemplateSelector.SelectedIndexChanged` 有綁到上述事件。

---

## 十二、可選：增加目前樣板提示文字

### 可做可不做

若版面允許，可增加提示，例如：

* 目前設定的是：低年級
* 點選「報表樣版設定」將修改目前選取樣板

### 目的

避免使用者以為自己改的是共用樣板。

若不想增加新 label，也可以保留不做。

---

## 十三、保留既有邏輯，不要修改的部分

以下邏輯本次不要動：

1. DataTable 欄位建立
2. 成績資料讀取與整理
3. MailMerge.Execute
4. ConvertToPDF 邏輯
5. OneFileSave 邏輯
6. 檔名命名邏輯
7. 其他欄位總表與 merge 欄位相關功能

本次只調整：

* 樣板儲存方式
* 樣板選擇 UI
* 列印時選哪個樣板

---

## 十四、建議補強：避免 null crash

### 若目前程式任何地方仍有直接使用

```csharp
Preference.Template.GetStream()
```

或

```csharp
某個TemplatePreference.Template.GetStream()
```

請改成統一走：

```csharp
GetCurrentTemplateStream()
```

### 原則

不要讓 Template 為 null 時直接呼叫 GetStream()。

---

## 十五、測試項目

### 基本測試

1. 開啟畫面後預設為 低年級
2. 點選 低年級 設定一份 doc 樣板，儲存成功
3. 切換到 中高年級，再設定另一份不同樣板，儲存成功
4. 切換到 自訂報表，再設定第三份樣板，儲存成功

### 隔離測試

5. 低年級 / 中高年級 / 自訂報表 不會互相覆蓋
6. 切回 低年級 時，仍保留低年級 原本設定
7. 切回 中高年級 時，仍保留中高年級 原本設定
8. 切回 自訂報表 時，仍保留自訂報表 原本設定

### 列印測試

9. 選擇 低年級 列印，確認輸出使用低年級
10. 選擇 中高年級 列印，確認輸出使用中高年級
11. 選擇 自訂報表 列印，確認輸出使用自訂報表

### 安全測試

12. 若某個樣板未設定自訂檔，仍可使用預設樣板列印，不可 crash
13. PDF 輸出正常
14. Word 輸出正常
15. 單檔列印正常
16. 多位學生列印資料不受影響
17. MailMerge 欄位內容不受影響

---

## 十六、完成後紀錄

修改完成後，請新增或更新：

國小在校成績證明書調整0421.md

### 記錄內容建議

1. PrintForm_StudentScoreCertificattion 新增 3 組獨立樣板設定
2. 新增畫面下拉選單，可手動選擇 低年級 / 中高年級 / 自訂報表
3. 列印時依目前選擇樣板輸出
4. 樣板設定按鈕改為設定目前選擇的樣板
5. 保留既有 MailMerge、PDF、單檔輸出與資料流邏輯
6. 補上樣板為 null 時的安全預設處理，避免 GetStream() 例外

---

## 十七、實作提醒

1. 變數命名請避免與原本 Preference 衝突
2. 不要刪除原本 Preference，因為它可能還在管理其他列印設定
3. 若 Designer 自動產生位置跑掉，請手動微調版面，但不要更動其他控制項功能
4. 若表單高度不足，再小幅調整 ClientSize
5. 若目前使用者習慣從「報表樣版設定」直接改樣板，請確保它現在是改目前選到的樣板，不要改錯目標

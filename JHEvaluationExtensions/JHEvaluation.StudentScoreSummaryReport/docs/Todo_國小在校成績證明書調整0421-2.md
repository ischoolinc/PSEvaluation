# 任務
在 Util.cs 新增「取得科目資料管理」相關方法
參考 J_Util.cs 作法
先完成 Util.cs，後續再讓 PrintForm_StudentScoreCertificattion.cs 排序使用

# 修改檔案
1. Util.cs

# 完成紀錄
國小在校成績證明書調整0421.md

---

## 一、修改目標

目前 Util.cs 沒有：
1. SubjectOrdinalInfo 類別
2. 取得科目資料管理清單的方法
3. 取得 科目名稱 -> 排序 的 Dictionary 方法
4. 取得 科目名稱 -> 英文名稱 的 Dictionary 方法

本次要先補上這些方法，供後續報表排序使用。

---

## 二、參考來源

請參考：
J_Util.cs

重點參考以下 3 個方法與 1 個 class：

1. SubjectOrdinalInfo
2. GetSubjectOrdinalList()
3. GetSubjectOrdinalDict()
4. GetSubjectEnglishNameDict()

---

## 三、保守修改原則

1. 只新增，不修改 Util.cs 既有方法行為
2. 不調整 Save / GetAllStudents / 其他既有功能
3. 不先修改 PrintForm_StudentScoreCertificattion.cs
4. 若讀取科目資料管理失敗，回傳空集合 / 空 Dictionary，不要 throw 影響報表其他功能

---

## 四、在 Util.cs 新增 SubjectOrdinalInfo 類別

放在：
internal static class Util
裡面，靠前位置即可

新增：

public class SubjectOrdinalInfo
{
    public int SubjectOrdinal { get; set; }
    public string SubjectName { get; set; }
    public string EnglishName { get; set; }
}

---

## 五、在 Util.cs 新增 GetSubjectOrdinalList()

方法名稱：
GetSubjectOrdinalList

用途：
讀取 list.name = 'JHEvaluation_Subject_Ordinal'
解析科目排序與英文名稱設定
回傳 List<SubjectOrdinalInfo>

請直接參考 J_Util.cs 的 SQL 與解析寫法，保守搬入。

重點：
1. 使用 QueryHelper
2. 來源 table = list
3. where name = 'JHEvaluation_Subject_Ordinal'
4. 先取 /Configurations/Configuration[@Name="SubjectOrdinal"]/text()
5. 文字內容再 decode 成 inner xml
6. 解析 /Subjects/Subject
7. 取出：
   - ordinal
   - @Name
   - @EnglishName

---

## 六、GetSubjectOrdinalList() 建議直接使用的邏輯

請盡量沿用 J_Util.cs，不要自行改 SQL 結構。

保留：
- try/catch
- 失敗回傳空 List
- 不中斷主流程

注意：
如果 Util.cs 目前沒有 using System.Xml;
本次其實 GetSubjectOrdinalList() 主要用 QueryHelper + DataTable，不一定需要額外 XmlDocument。
若編譯不需要，就先不要多加無用 using。

---

## 七、在 Util.cs 新增 GetSubjectOrdinalDict()

方法名稱：
GetSubjectOrdinalDict

用途：
取得 科目名稱 -> 排序 的 Dictionary<string, int>

邏輯：
1. 呼叫 GetSubjectOrdinalList()
2. 依 SubjectName 建 Dictionary
3. 若 SubjectName 空白，略過
4. 若重複名稱，保留第一筆，不重複 Add

新增：

public static Dictionary<string, int> GetSubjectOrdinalDict()
{
    Dictionary<string, int> value = new Dictionary<string, int>();

    foreach (SubjectOrdinalInfo info in GetSubjectOrdinalList())
    {
        if (!string.IsNullOrEmpty(info.SubjectName) && !value.ContainsKey(info.SubjectName))
            value.Add(info.SubjectName, info.SubjectOrdinal);
    }

    return value;
}

---

## 八、在 Util.cs 新增 GetSubjectEnglishNameDict()

方法名稱：
GetSubjectEnglishNameDict

用途：
取得 科目名稱 -> 英文名稱 的 Dictionary<string, string>

邏輯：
1. 呼叫 GetSubjectOrdinalList()
2. 依 SubjectName 建 Dictionary
3. 若 SubjectName 空白，略過
4. 若重複名稱，保留第一筆

新增：

public static Dictionary<string, string> GetSubjectEnglishNameDict()
{
    Dictionary<string, string> value = new Dictionary<string, string>();

    foreach (SubjectOrdinalInfo info in GetSubjectOrdinalList())
    {
        if (!string.IsNullOrEmpty(info.SubjectName) && !value.ContainsKey(info.SubjectName))
            value.Add(info.SubjectName, info.EnglishName);
    }

    return value;
}

---

## 九、注意命名與衝突

1. 確認 Util.cs 內目前沒有同名：
   - SubjectOrdinalInfo
   - GetSubjectOrdinalList
   - GetSubjectOrdinalDict
   - GetSubjectEnglishNameDict

2. 若已有類似舊方法，不要直接覆蓋舊方法邏輯
   先確認呼叫點，再決定是新增或整併

---

## 十、注意 namespace / using

Util.cs 目前已有：
using FISCA.Data;
using System.Data;

這對 QueryHelper / DataTable 已足夠。

若編譯需要，再補必要 using。
不要一次加太多無用 using。

---

## 十一、測試重點

完成後先在 Util.cs 層級確認：

1. 可成功編譯
2. GetSubjectOrdinalList() 可正常回傳資料
3. GetSubjectOrdinalDict() 可取得 科目名稱 -> 排序
4. GetSubjectEnglishNameDict() 可取得 科目名稱 -> 英文名稱
5. 若 list 沒資料或解析失敗，不會 throw 影響主流程，只回傳空集合

---

## 十二、這次先不要做的事

本次先不要修改：
1. PrintForm_StudentScoreCertificattion.cs
2. 報表排序呼叫點
3. 領域清單
4. 各領域最大科目數
5. 科目欄位輸出

這次只先把 Util.cs 補齊。

---

## 十三、下一步預留

Util.cs 完成後，下一步再修改：
PrintForm_StudentScoreCertificattion.cs

後續排序請直接使用：
Util.GetSubjectOrdinalDict()

排序原則：
1. 先依科目資料管理排序
2. 沒設定排序的科目放後面
3. 同排序值再依科目名稱排序

---

## 十四、完成紀錄

修改完成後，請記錄在：

國小在校成績證明書調整0421.md

建議紀錄內容：

1. 在 Util.cs 新增 SubjectOrdinalInfo 類別
2. 新增 GetSubjectOrdinalList()，可讀取 list.name = 'JHEvaluation_Subject_Ordinal'
3. 新增 GetSubjectOrdinalDict()，提供報表科目排序使用
4. 新增 GetSubjectEnglishNameDict()，提供科目英文名稱對照使用
5. 採保守方式，只新增方法，不影響既有 Util.cs 功能
